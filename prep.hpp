#ifndef PREP_
#define PREP_

#include "node.h"

                        // we are not using data types (strings, int, etc.)... 
                        // we are using template instead 
template <class T>

class Prep
{
        private:

                node<T> *head;
                

        public:
};

#endif


